name: Swift CI

# Builds, lints and tests the Swift package on the GitHub-hosted macOS runner
# (the Forge runner takes over through `MACOS_RUNS_ON` once it is registered).
# Path-gated so it does nothing for unrelated changes.
#
# What every run reports (see `.plans/2026-09-25-v1-program.md`, verification
# standard): `swift format lint --strict`, `swift test --parallel
# --xunit-output`, a step summary with totals and every skipped test's name and
# message, and a failure when a skip message names none of the `STENO_*`
# opt-in switches. `.build` is cached on `Package.resolved`, the Swift version
# and a hash of the sources, with the resolved-only key as a fallback, so a
# run never restores exactly the state of an earlier tree. Sixty minutes
# because FluidAudio and WhisperKit compile cold in 15 to 25 minutes once
# their workstreams land.

on:
  pull_request:
    paths:
      - 'Package.swift'
      - 'Package.resolved'
      - 'Sources/**'
      - 'Tests/**'
      - 'apps/macos/**'
      - '.github/workflows/swift-ci.yml'
  push:
    branches: ['main']
    paths:
      - 'Package.swift'
      - 'Package.resolved'
      - 'Sources/**'
      - 'Tests/**'
      - 'apps/macos/**'
      - '.github/workflows/swift-ci.yml'

concurrency:
  group: swift-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  package:
    name: Build and test package
    # MACOS_RUNS_ON holds a JSON value: the string "macos-15" today, a label
    # array such as ["self-hosted","macOS","ARM64"] once the Forge runner is
    # registered. fromJSON turns either into a runner label.
    runs-on: ${{ fromJSON(vars.MACOS_RUNS_ON || '"macos-15"') }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      # Pinned, not `latest-stable`: the toolchain decides how `swift format`
      # lays code out and which Swift Testing / xunit shape we parse below.
      # Xcode 16.4 is Swift 6.1, the version GRDB 7.11 requires.
      # Hosted runners only: the self-hosted Forge runner ships one Xcode and
      # no 16.4, so there we rely on the preinstalled toolchain.
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.environment == 'github-hosted'
        with:
          xcode-version: '16.4'

      - name: Swift version
        id: swift
        run: |
          swift --version
          version="$(swift --version 2>&1 | head -n 1 | tr -c 'A-Za-z0-9.\n' '-')"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Cache .build
        uses: actions/cache@v4
        with:
          path: .build
          key: swift-build-${{ runner.os }}-${{ steps.swift.outputs.version }}-${{ hashFiles('Package.resolved') }}-${{ hashFiles('Package.swift', 'Sources/**', 'Tests/**') }}
          restore-keys: |
            swift-build-${{ runner.os }}-${{ steps.swift.outputs.version }}-${{ hashFiles('Package.resolved') }}-
            swift-build-${{ runner.os }}-${{ steps.swift.outputs.version }}-

      - name: Lint formatting
        run: swift format lint --strict --recursive Sources Tests Package.swift

      - name: Build
        run: swift build --build-tests

      # The reports live outside the cached `.build`, so a run whose test step
      # dies before writing can never summarise an earlier run's XML.
      - name: Test
        id: test
        run: |
          rm -rf test-results && mkdir -p test-results
          swift test --parallel --skip-build --xunit-output test-results/junit.xml

      # `--xunit-output` writes XCTest results to junit.xml and Swift Testing
      # results to junit-swift-testing.xml (a `<skipped>` element carries the
      # skip comment). Totals and every skip land in the step summary; a skip
      # whose message names no `STENO_*` opt-in switch fails the job, so an
      # accidental skip cannot hide.
      - name: Test summary
        if: success() || steps.test.outcome == 'failure'
        run: |
          python3 - <<'PY'
          import glob, os, sys
          import xml.etree.ElementTree as ET

          files = sorted(glob.glob("test-results/junit*.xml"))
          totals = {"tests": 0, "failures": 0, "errors": 0, "skipped": 0}
          skipped, failed = [], []
          for path in files:
              root = ET.parse(path).getroot()
              suites = [root] if root.tag == "testsuite" else root.iter("testsuite")
              for suite in suites:
                  for key in totals:
                      totals[key] += int(suite.get(key, "0") or 0)
              for case in root.iter("testcase"):
                  name = f'{case.get("classname", "")}/{case.get("name", "")}'
                  skip = case.find("skipped")
                  if skip is not None:
                      message = (skip.get("message") or skip.text or "").strip()
                      skipped.append((name, message))
                  if case.find("failure") is not None or case.find("error") is not None:
                      failed.append(name)

          lines = ["## Swift tests", ""]
          if not files:
              lines.append("No xunit output found under `test-results/junit*.xml`.")
          lines.append("| Tests | Failures | Errors | Skipped |")
          lines.append("|---|---|---|---|")
          lines.append(f'| {totals["tests"]} | {totals["failures"]} | {totals["errors"]} | {totals["skipped"]} |')
          lines.append("")
          if failed:
              lines.append("### Failed")
              lines.extend(f"- `{name}`" for name in failed)
              lines.append("")
          if skipped:
              lines.append("### Skipped")
              lines.extend(f"- `{name}`: {message or '(no message)'}" for name, message in skipped)
              lines.append("")
          for source in files:
              lines.append(f"Source: `{source}`")
          summary = "\n".join(lines) + "\n"
          print(summary)
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a") as handle:
              handle.write(summary)

          bad = [name for name, message in skipped if "STENO_" not in message]
          if bad:
              print("::error::skipped tests whose message names no STENO_* opt-in switch: " + ", ".join(bad))
              sys.exit(1)
          if not files:
              print("::error::no xunit output was produced")
              sys.exit(1)
          PY

      # A golden mismatch writes the actual bytes beside the golden as
      # `<name>.actual` (see StenoCore/Testing/Snapshot.swift); uploading them
      # lets a developer without a Mac inspect or adopt the runner's output.
      # The junit files must exist, so a missing upload fails the job instead
      # of passing silently.
      - name: Upload xunit output and snapshot mismatches
        if: success() || steps.test.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: test-output
          path: |
            test-results/junit*.xml
            Tests/Fixtures/**/*.actual
          if-no-files-found: error

  # The macOS app: xcodegen generates the project (git-ignored), xcodebuild
  # compiles the app against the local package and Sparkle with an ad-hoc
  # signature (Debug: hardened runtime off, so no certificate is needed),
  # then the hostless unit tests and the UI smoke test run separately so a
  # UI-automation failure on the hosted runner (spike S1) is attributable.
  # Runs beside `package`, not after it, so a lint failure there never hides
  # an app build failure and the two long builds overlap. DerivedData is
  # cached so FluidAudio and WhisperKit compile once per dependency change.
  app:
    name: Build and test macOS app
    runs-on: ${{ fromJSON(vars.MACOS_RUNS_ON || '"macos-15"') }}
    timeout-minutes: 90
    env:
      XCODEBUILD_FLAGS: >-
        -project apps/macos/Steno.xcodeproj
        -scheme Steno
        -configuration Debug
        -destination platform=macOS,arch=arm64
        -derivedDataPath apps/macos/build/DerivedData
        -skipPackagePluginValidation
        -skipMacroValidation
        CODE_SIGN_IDENTITY=-
        CODE_SIGN_STYLE=Manual
        DEVELOPMENT_TEAM=
    steps:
      - uses: actions/checkout@v4

      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.environment == 'github-hosted'
        with:
          xcode-version: '16.4'

      - name: Swift version
        id: swift
        run: |
          swift --version
          version="$(swift --version 2>&1 | head -n 1 | tr -c 'A-Za-z0-9.\n' '-')"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Install xcodegen
        run: apps/macos/scripts/install-xcodegen.sh

      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: |
            apps/macos/build/DerivedData/SourcePackages
            apps/macos/build/DerivedData/Build
          key: steno-app-${{ runner.os }}-${{ steps.swift.outputs.version }}-${{ hashFiles('apps/macos/project.yml', 'Package.swift') }}-${{ hashFiles('Sources/**', 'apps/macos/**') }}
          restore-keys: |
            steno-app-${{ runner.os }}-${{ steps.swift.outputs.version }}-${{ hashFiles('apps/macos/project.yml', 'Package.swift') }}-
            steno-app-${{ runner.os }}-${{ steps.swift.outputs.version }}-

      - name: Generate project
        run: |
          xcodegen generate --spec apps/macos/project.yml
          test -d apps/macos/Steno.xcodeproj
          plutil -lint apps/macos/Steno/Info.plist

      - name: Resolve packages
        run: |
          # shellcheck disable=SC2086
          xcodebuild -resolvePackageDependencies $XCODEBUILD_FLAGS

      # xcodebuild's full log goes to a file (uploaded below); the console
      # shows compiler diagnostics and the result line only.
      - name: Build for testing
        run: |
          set -o pipefail
          mkdir -p apps/macos/build
          # shellcheck disable=SC2086
          xcodebuild build-for-testing $XCODEBUILD_FLAGS 2>&1 | tee apps/macos/build/build.log | grep -E --line-buffered '(error:|warning: .*apps/macos|\*\* .* \*\*)' || true
          grep -q '\*\* TEST BUILD SUCCEEDED \*\*' apps/macos/build/build.log

      - name: Unit tests
        id: unit
        run: |
          set -o pipefail
          # shellcheck disable=SC2086
          xcodebuild test-without-building $XCODEBUILD_FLAGS \
            -only-testing:StenoTests \
            -resultBundlePath apps/macos/build/unit.xcresult 2>&1 | tee apps/macos/build/unit.log | grep -E --line-buffered '(Test Case|Test Suite|error:|\*\* )' || true
          grep -q '\*\* TEST EXECUTE SUCCEEDED \*\*' apps/macos/build/unit.log

      # Spike S1: XCUITest on the hosted runner. If this step fails with the
      # automation-mode timeout, the smoke test becomes Forge-only and the
      # summary lists it as skipped with that reason.
      - name: UI smoke test
        id: ui
        if: success() || steps.unit.outcome == 'failure'
        run: |
          set -o pipefail
          # shellcheck disable=SC2086
          xcodebuild test-without-building $XCODEBUILD_FLAGS \
            -only-testing:StenoUITests \
            -resultBundlePath apps/macos/build/ui.xcresult 2>&1 | tee apps/macos/build/ui.log | grep -E --line-buffered '(Test Case|Test Suite|error:|\*\* )' || true
          grep -q '\*\* TEST EXECUTE SUCCEEDED \*\*' apps/macos/build/ui.log

      - name: Test summary
        if: always()
        run: python3 apps/macos/scripts/xcresult-summary.py apps/macos/build/unit.xcresult apps/macos/build/ui.xcresult

      - name: Upload logs and result bundles
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: macos-app-output
          path: |
            apps/macos/build/*.log
            apps/macos/build/*.xcresult
          if-no-files-found: warn
