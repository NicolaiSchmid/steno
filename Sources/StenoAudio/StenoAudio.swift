/// Placeholder namespace for the `StenoAudio` module. Its workstream replaces
/// this file with the real implementation; the core foundation only creates
/// the target so the package builds.
public enum StenoAudio {}
